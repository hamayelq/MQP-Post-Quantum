import { ntru } from 'ntru';
// import { getKeyPair } from '../constants';

async function NTRUencode(plaintext: string): Promise<any> {

    // const response = await getKeyPair();
    // let keyPair = response;
    // const uintArray = new TextEncoder().encode(plaintext);
    // const encrypted = await ntru.encrypt(uintArray, keyPair.publicKey);
    // return encrypted;
}


export default NTRUencode;