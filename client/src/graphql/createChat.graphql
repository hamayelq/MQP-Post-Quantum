mutation CreateChat(
  $memberIds: [String!]!
  $userId: String!
  $sentBySymKey: String!
  $acceptedBySymKey: String!
) {
  createChat(
    memberIds: $memberIds
    userId: $userId
    sentBySymKey: $sentBySymKey
    acceptedBySymKey: $acceptedBySymKey
  )
}
