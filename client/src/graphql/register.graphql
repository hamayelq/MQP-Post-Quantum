mutation Register(
  $email: String!
  $username: String!
  $password: String!
  $publicKey: String!
  $encryptedPrivateKey: String!
) {
  register(
    email: $email
    username: $username
    password: $password
    publicKey: $publicKey
    encryptedPrivateKey: $encryptedPrivateKey
  )
}
