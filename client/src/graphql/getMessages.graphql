query GetMessages($chatId: String!) {
  getMessages(chatId: $chatId) {
    chat {
      uuid
      messages {
        uuid
        sender {
          uuid
          username
        }
        content
        me
      }
    }
  }
}
